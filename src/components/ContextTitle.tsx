import React from "react";

const ContextTitle = ({ children }: { children: React.ReactNode }) => {
  return <p className="text-text2 text-xl font-semibold mb-3">{children}</p>;
};

export default ContextTitle;
